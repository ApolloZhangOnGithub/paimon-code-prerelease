# steam 目录规范

## 文件结构
- `steam.ts` — 主入口：GAMES 注册表、游戏菜单、路由分发、MobileApp 导出
- 每个游戏一个 `.ts` 文件，导出游戏类（如 `GomokuGame`、`ChessGame`）
- 不另建子目录，不另建辅助文件，不另建 .js 桥接

## 游戏注册
- `steam.ts` 顶部静态 import 所有游戏类
- `GAMES` const 数组为菜单唯一数据源（不用外部 JSON）
- `SteamState` 统一管理所有游戏实例状态
- 不创建独立 MobileApp 注册到 kernel

## 联机
- 联机走 `system.server/game-server.ts` 统一服务器
- 游戏客户端 fetch 逻辑放游戏 `.ts` 内（如 `gomokuOnline`）
